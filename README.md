# Indigo

Indigo is a lightweight fitness and habit tracker that runs entirely in the
browser.

## Run locally

Use the included script:

```sh
./Run.command
```

Then open:

```
http://localhost:5173
```

## Notes

- Data is stored locally in the browser (IndexedDB + localStorage).
- If you change the service worker, hard refresh to update the cache.
